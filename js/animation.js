
    document.addEventListener('DOMContentLoaded', function() {
    var elementToAnimate = document.querySelector('.animate-me');

    // Déclencher l'animation
    elementToAnimate.style.opacity = 1;
    elementToAnimate.style.top = '0px';
    });